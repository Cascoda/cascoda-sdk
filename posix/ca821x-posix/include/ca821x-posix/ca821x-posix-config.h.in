/**
 * CA821X_ASYNC_CALLBACK enables the asynchronous, automatic calling of the
 * callback functions from a secondary thread. If synchronous polling of
 * indications is required, then ca821x_util_dispatch_poll should
 * be regularly called from a polling loop.
 */
#cmakedefine01 CA821X_ASYNC_CALLBACK

/**
 * CASCODA_RASPI_USB_WORKAROUND enables the workaround for the raspberry pi
 * USB issue that causes occasional high latency USB transfers. Only required
 * for raspberry pi version 3 and lower.
 */
#cmakedefine01 CASCODA_RASPI_USB_WORKAROUND
